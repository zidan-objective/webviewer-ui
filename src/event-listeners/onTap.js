import core from 'core';

export default dispatch => async e => {
  // const annot = core.getAnnotationByMouseEvent(e);
  // console.log(annot);
  // if (annot instanceof Annotations.SignatureWidgetAnnotation && await annot.isSignedDigitally()) {
  //   console.log('here');
  // }
};